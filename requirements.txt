# requirements.txt — full install for local development
# For production, install only what you need via pyproject.toml extras

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.1

# LLM Backends (install only what you need)
ollama>=0.2.1               # local models via Ollama
# openai>=1.30.0            # OpenAI / Azure OpenAI
# anthropic>=0.28.0         # Anthropic Claude
# huggingface_hub>=0.23.0   # HuggingFace Inference API

# Dev / testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.4
